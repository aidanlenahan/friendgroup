{
  "name": "friendgroup",
  "version": "1.0.0",
  "private": true,
  "description": "Friendgroup monorepo",
  "scripts": {
    "dev": "npm-run-all -p dev:api dev:web",
    "dev:api": "npm --workspace apps/api run dev",
    "dev:web": "npm --workspace apps/web run dev",
    "build": "npm run build --workspace=apps/api && npm run build --workspace=apps/web",
    "build:api": "npm --workspace apps/api run build",
    "build:web": "npm --workspace apps/web run build",
    "lint": "npm run -ws --if-present lint",
    "typecheck": "npm run -ws --if-present typecheck",
    "test": "npm run test --workspaces --if-present",
    "db:migrate": "npm run prisma migrate dev --workspace=apps/api",
    "db:seed": "npm run db:seed --workspace=apps/api",
    "db:studio": "npm run prisma studio --workspace=apps/api"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/aidanlenahan/friendgroup.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/aidanlenahan/friendgroup/issues"
  },
  "homepage": "https://github.com/aidanlenahan/friendgroup#readme",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}

# GEM — Active Todo
Last updated: 2026-05-27

Pending work only. Completed items live in tasks.txt.

================

## Infrastructure

- [ ] Step 16: PBS backup integration + restore test

================

## UX / Mobile

- [ ] keyboard overlap on mobile (tag edit, group manage modals — extends Modal.tsx viewport fix already done for group creation/join)
- [ ] mobile channel view — when keyboard opens, shrink header and slide it toward top border so message input stays visible
- [ ] no way to pin or delete messages on mobile — add long-press action sheet (pin, delete, react, reply) matching desktop hover bar
- [ ] desktop channel page — inconsistent div/window sizing causes layout overflow in some page combinations; audit page wrappers
- [ ] Photos tab label clipped on mobile (390px) — 4-tab layout overflows; shorten to "Media" or use icon

================

## Features

- [ ] reply function on swipe-right gesture for mobile channel messages
- [ ] confirm message pin functionality end-to-end (edge cases: unpin, scroll-to-pinned, multi-pin ordering)
- [ ] post demo page — accessible without an account; link from login page; note directing feedback to help@gem.aidanlenahan.com
- [ ] PWA install guide for iOS and Android — autodetect platform, prompt if not installed (partial)
- [ ] text sizing / accessibility settings per user
- [ ] recurring events (RRULE support; needs recurrenceRule on Event + RRULE UI)
- [ ] intuitive usage hints — e.g., tooltip or empty-state note that swipe-right on mobile opens chat actions

================

## Won't Fix

- Google Calendar URL subscription shows feed as "Other calendars" sidebar — by-design GCal behavior for all external URL subscriptions; webcal:// is the better path. Optional: add a brief UI note so users know this is expected.

================

## Later / Stretch

- [ ] waitlist to make account (public landing page signup form)
- [ ] social functionality — friends via invite link, global username search, mutual groups
- [ ] verify account via alternative channel (WhatsApp, Telegram, SMS)
- [ ] end-to-end encrypted chats
